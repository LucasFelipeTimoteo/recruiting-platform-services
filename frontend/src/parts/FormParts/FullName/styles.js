import { makeStyles } from "@material-ui/core";

const useFullnameStyles = makeStyles({
  label: {
    margin: 10,
    color: 'gray',
    fontWeight: 500,
    fontSize: '1rem'
  }
})

export default useFullnameStyles