import { makeStyles } from "@material-ui/core";

const useTimerInfoTextSubtitleStyles = makeStyles({
  timerInfoTextSubtitle: {
    color: '#5E4429',
    letterSpacing: 0,
    textAlign: 'center',
    width: '60%'
  }
})

export default useTimerInfoTextSubtitleStyles