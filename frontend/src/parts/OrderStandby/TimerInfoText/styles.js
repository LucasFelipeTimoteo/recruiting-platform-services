import { makeStyles } from "@material-ui/core";

const useTimerInfoTextStyles = makeStyles({
  timerInfoTextContainer: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    width: 220
  }
})

export default useTimerInfoTextStyles