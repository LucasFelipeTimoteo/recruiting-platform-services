import { makeStyles } from "@material-ui/core";

const useSuccessTitleStyles = makeStyles({
  successTitle: {
    textAlign: 'center',
    margin: '40px 0 30px 0'
  }
})

export default useSuccessTitleStyles