import { makeStyles } from "@material-ui/core";

const useTimerInfoTextTitleStyles = makeStyles({
  timerInfoTextTitle: {
    margin: '30px 0 10px 0',
    fontWeight: 600,
    color: '#5E4429',
    letterSpacing: 0
  }
})

export default useTimerInfoTextTitleStyles