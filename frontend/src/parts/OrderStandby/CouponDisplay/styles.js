import { makeStyles } from "@material-ui/core";

const useCouponDisplayStyles = makeStyles({
  couponContainer: {
    width: '100%',
    border: '5px dashed #b0b0b0',
    padding: 10,
    textAlign: 'center',
    marginTop: 'auto'
  }
})

export default useCouponDisplayStyles