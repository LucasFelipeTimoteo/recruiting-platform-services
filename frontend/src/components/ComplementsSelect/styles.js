import { makeStyles } from "@material-ui/core";

const useComplementsSelectStyles = makeStyles({
  ComplementsSelectWrapper: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    width: '100%',
    maxWidth: 800,
    height: '100%',
  },
  listTitle: {
    margin: 17
  }
})

export default useComplementsSelectStyles