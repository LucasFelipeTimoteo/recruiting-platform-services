import { makeStyles } from "@material-ui/core";

const useCoffeeSelectStyles = makeStyles({
  coffeeSelectWrapper: {
    width: '100%',
    height: '100vh',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  }
})

export default useCoffeeSelectStyles