import displayCounterTime from "./utils/displayCounterTime"

const timerDisplay = (remainingTime) => {
  const counterTime = displayCounterTime(remainingTime)

  return counterTime
}

export default timerDisplay