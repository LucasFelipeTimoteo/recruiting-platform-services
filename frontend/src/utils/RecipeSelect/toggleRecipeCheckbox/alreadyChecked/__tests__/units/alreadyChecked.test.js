describe('only return true is checked property is true, else, return false', () => {
  test('should return true', () => {
    const expected = true
    const checked = true

    expect(expected).toBe(checked)
  })
})
